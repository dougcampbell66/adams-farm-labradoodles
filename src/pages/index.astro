---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPuppies = await getCollection('puppies');
const springPuppies = allPuppies
  .filter(p => p.data.litter === 'spring-2026')
  .sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99));
---
<Layout title="Home">

  <!-- ─── HERO (unchanged) ─────────────────────────────────────── -->
  <svg width="0" height="0" style="position:absolute;pointer-events:none" aria-hidden="true">
    <defs>
      <clipPath id="hero-blob" clipPathUnits="objectBoundingBox">
        <path d="M 0.53,0.02 C 0.79,0.00 0.99,0.22 0.97,0.52 C 0.96,0.79 0.77,0.98 0.50,0.97 C 0.23,0.96 0.02,0.77 0.03,0.50 C 0.04,0.23 0.25,0.03 0.53,0.02 Z"/>
      </clipPath>
    </defs>
  </svg>

  <section class="hero-section">
    <div class="container hero-grid">
      <div class="hero-text">
        <p class="eyebrow">Australian Labradoodles</p>
        <h1>Raised in Loving Homes. Ready for Yours.</h1>
        <p class="hero-copy">Every litter comes from fully health-tested parents and grows up surrounded by family, before going to yours.</p>
        <div class="hero-actions">
          <a href="/puppies" class="btn btn-primary">See available puppies</a>
          <a href="/our-dogs" class="btn btn-secondary">Meet the parents</a>
        </div>
      </div>
      <div class="hero-photo-frame">
        <div class="hero-photo-masked">
          <img
            src="/images/hero/founder-hero-portrait.jpg"
            alt="A girl holding an Adams Farm Labradoodle puppy"
            class="hero-photo-img"
          />
        </div>
        <div class="hero-testimonial">
          <p class="ht-quote">&ldquo;We are smitten with our bright &amp; adorable Australian Labradoodle.&rdquo;</p>
          <p class="ht-attr">&mdash; Deirdre</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ① PUPPIES: horizontal scroll carousel -->
  <section class="s-puppies">
    <div class="container s-puppies-hd">
      <p class="eyebrow">Available Puppies</p>
      <h2 class="puppies-heading">Meet Our Lilo &amp; Stitch May Litter</h2>
      <p class="puppies-sub">These practically perfect puppies come from Legend Manor&rsquo;s Holly and Tarheel&rsquo;s Knox, and are expected to mature into large minis between 20 and 25 lbs.</p>
      <a href="/our-dogs" class="puppies-parents-link">Meet the Parents &rarr;</a>
    </div>
    <div class="container puppies-carousel-wrap">
      <button class="carousel-btn carousel-btn-prev" id="puppy-prev" aria-label="Scroll left">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <div class="s-scroll" id="puppy-scroll">
        {springPuppies.map((p) => (
          <div class="puppy-card" data-status={p.data.status}>
            <div class="puppy-card-photo">
              <img src={p.data.photo} alt={p.data.name} loading="lazy" />
            </div>
            <div class="puppy-card-body">
              <span class="puppy-name">{p.data.name}</span>
              {p.data.collar && <span class="puppy-collar">{p.data.collar}</span>}
              <div class="puppy-badges">
                {p.data.sex && <span class="puppy-sex">{p.data.sex}</span>}
                <span class={`litter-status litter-status-${p.data.status}`}>
                  {p.data.status.charAt(0).toUpperCase() + p.data.status.slice(1)}
                </span>
              </div>
            </div>
          </div>
        ))}
      </div>
      <button class="carousel-btn carousel-btn-next" id="puppy-next" aria-label="Scroll right">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 18l6-6-6-6"/></svg>
      </button>
    </div>
    <div class="container puppies-cta">
      <a href="/contact" class="btn btn-primary">Reserve a Puppy</a>
    </div>
  </section>

  <script>
    const track = document.getElementById('puppy-scroll') as HTMLElement | null;
    const btnPrev = document.getElementById('puppy-prev') as HTMLButtonElement | null;
    const btnNext = document.getElementById('puppy-next') as HTMLButtonElement | null;

    function updateArrows() {
      if (!track || !btnPrev || !btnNext) return;
      btnPrev.style.display = track.scrollLeft <= 1 ? 'none' : 'flex';
      btnNext.style.display = track.scrollLeft + track.clientWidth >= track.scrollWidth - 1 ? 'none' : 'flex';
    }

    function cardStep() {
      const card = track?.querySelector('.puppy-card') as HTMLElement | null;
      return card ? card.offsetWidth + 20 : 260;
    }

    btnPrev?.addEventListener('click', () => {
      track?.scrollBy({ left: -cardStep(), behavior: 'smooth' });
    });
    btnNext?.addEventListener('click', () => {
      track?.scrollBy({ left: cardStep(), behavior: 'smooth' });
    });
    track?.addEventListener('scroll', updateArrows, { passive: true });
    updateArrows();
  </script>

  <!-- NEW ─── PROGRAM: 4-card phase framework ────────────────── -->
  <section class="s-program section">
    <div class="container s-section-header">
      <p class="eyebrow">Our Program</p>
      <h2>The PuppyQ Developmental Framework</h2>
      <p class="s-program-intro">PuppyQ &mdash; Q for Quotient &mdash; is a structured, evidence-based approach to raising puppies from birth through their first year of life. Adams Farm raises every litter using the PuppyQ framework. Temperament and resilience aren&rsquo;t left to chance: every litter follows four defined stages, each building on the one before it, so puppies arrive genuinely ready for real family life.</p>
    </div>
    <div class="container s-program-grid">
      <div class="prog-card prog-card-green" tabindex="0">
        <div class="prog-card-inner">
          <div class="prog-face prog-face-front">
            <p class="prog-phase"><span class="prog-badge">1</span>Stage 1</p>
            <p class="prog-timeframe">Days 3–16</p>
            <h3 class="prog-title">Neurological Q</h3>
            <p class="prog-front-body">Building early resilience.</p>
          </div>
          <div class="prog-face prog-face-back">
            <p class="prog-back-body">The foundation stage. Before the fear response develops, we use Early Neurological Stimulation and early scent introduction to build the raw hardware for resilience and focus.</p>
          </div>
        </div>
      </div>
      <div class="prog-card prog-card-blue" tabindex="0">
        <div class="prog-card-inner">
          <div class="prog-face prog-face-front">
            <p class="prog-phase"><span class="prog-badge">2</span>Stage 2</p>
            <p class="prog-timeframe">Weeks 3–6</p>
            <h3 class="prog-title">Emotional Q</h3>
            <p class="prog-front-body">Learning calm and regulation.</p>
          </div>
          <div class="prog-face prog-face-back">
            <p class="prog-back-body">As the socialization window opens and weaning begins, puppies learn emotional regulation through safe outings, startle-and-recovery exercises, and guided frustration tolerance as they separate from mom.</p>
          </div>
        </div>
      </div>
      <div class="prog-card prog-card-yellow" tabindex="0">
        <div class="prog-card-inner">
          <div class="prog-face prog-face-front">
            <p class="prog-phase"><span class="prog-badge">3</span>Stage 3</p>
            <p class="prog-timeframe">Weeks 6–9</p>
            <h3 class="prog-title">Social Q</h3>
            <p class="prog-front-body">Meeting the world, by design.</p>
          </div>
          <div class="prog-face prog-face-back">
            <p class="prog-back-body">Focus shifts outward. Puppies work toward Dr. Ian Dunbar&rsquo;s &ldquo;100 in 12&rdquo; benchmark, typically hitting it by 9 weeks, while learning foundational manners, crate conditioning, and an informal Week 7 snapshot.</p>
          </div>
        </div>
      </div>
      <div class="prog-card prog-card-red" tabindex="0">
        <div class="prog-card-inner">
          <div class="prog-face prog-face-front">
            <p class="prog-phase"><span class="prog-badge">4</span>Stage 4</p>
            <p class="prog-timeframe">Months 2–12</p>
            <h3 class="prog-title">Individual Q</h3>
            <p class="prog-front-body">Finished by your family.</p>
          </div>
          <div class="prog-face prog-face-back">
            <p class="prog-back-body">The transition from puppy to adult. Started at Adams Farm and continued at home, this stage covers the real-world exposure, routines, and independence-building that shape who your dog becomes.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="container s-program-timeline">
      <svg viewBox="0 0 1000 220" xmlns="http://www.w3.org/2000/svg" style="width:100%; max-width:900px; height:auto; display:block; margin:2rem auto 0;">
        <line x1="125" y1="110" x2="875" y2="110" stroke="var(--color-border-dark)" stroke-width="3"/>
        <circle cx="125" cy="110" r="34" fill="var(--color-focus-green)"/>
        <circle cx="375" cy="110" r="34" fill="var(--color-calm-blue)"/>
        <circle cx="625" cy="110" r="34" fill="var(--color-excite-yellow)"/>
        <circle cx="875" cy="110" r="34" fill="var(--color-feelings-red)"/>
        <text x="125" y="117" text-anchor="middle" fill="#fff" font-size="22" font-weight="700">1</text>
        <text x="375" y="117" text-anchor="middle" fill="#fff" font-size="22" font-weight="700">2</text>
        <text x="625" y="117" text-anchor="middle" fill="#fff" font-size="22" font-weight="700">3</text>
        <text x="875" y="117" text-anchor="middle" fill="#fff" font-size="22" font-weight="700">4</text>
        <text x="125" y="170" text-anchor="middle" fill="var(--color-card)" font-size="15" font-weight="600">Days 3–16</text>
        <text x="375" y="170" text-anchor="middle" fill="var(--color-card)" font-size="15" font-weight="600">Weeks 3–6</text>
        <text x="625" y="170" text-anchor="middle" fill="var(--color-card)" font-size="15" font-weight="600">Weeks 6–9</text>
        <text x="875" y="170" text-anchor="middle" fill="var(--color-card)" font-size="15" font-weight="600">Months 2–12</text>
      </svg>
    </div>
  </section>

  <script>
    document.querySelectorAll('.prog-card').forEach(card => {
      card.addEventListener('click', () => {
        if (window.matchMedia('(hover: none)').matches) {
          card.classList.toggle('is-flipped');
        }
      });
    });
  </script>

  <!-- ② ABOUT ADAMS FARM + ③ TRUST: image left, text + 2×2 trust grid right -->
  <section class="s-about section">
    <div class="container s-about-inner">
      <div class="about-photo">
        <img
          src="/images/story/douglas-erika-winnie.jpg"
          alt="Douglas and Erika Campbell with Winnie"
          class="about-photo-img"
        />
      </div>
      <div class="s-about-text">
        <p class="eyebrow">Adams Farm Labradoodles</p>
        <h2>Where Every Puppy Grows Up Loved</h2>
        <p class="s-about-body">Adams Farm Labradoodles breeds and raises ALAA registered Australian Labradoodles. Our breeding dogs are raised in loving homes, health tested, and evaluated for therapy-dog temperament. Our puppies are raised to be calm and confident in new environments.</p>
        <a href="/our-program" class="trust-program-link">Learn more about our program &rarr;</a>
        <div class="trust-grid">
          <div class="trust-card">
            <svg class="trust-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
            <h3 class="trust-card-heading">ALAA Gold Paw Accredited</h3>
            <p class="trust-card-body">Our highest health-testing standard under the Australian Labradoodle Association of America.</p>
          </div>
          <div class="trust-card">
            <svg class="trust-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
            </svg>
            <h3 class="trust-card-heading">Early Neurological Stimulation</h3>
            <p class="trust-card-body">Starting at day 3, the same neurological exercises developed for military working dogs.</p>
          </div>
          <div class="trust-card">
            <svg class="trust-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              <polyline points="9 12 11 14 15 10"/>
            </svg>
            <h3 class="trust-card-heading">3-Year Health Guarantee</h3>
            <p class="trust-card-body">We test for known congenital defects and guarantee what can&rsquo;t be tested for, for three years.</p>
          </div>
          <div class="trust-card">
            <svg class="trust-icon" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
            </svg>
            <h3 class="trust-card-heading">Raised in Loving Homes</h3>
            <p class="trust-card-body">Every dog in our program lives with us or with a trusted guardian family &mdash; never in a kennel.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ⑤ WHY AUSTRALIAN LABRADOODLE -->
  <section class="s-why section">
    <div class="container s-why-inner">
      <div class="s-why-text">
        <p class="eyebrow">Authentic Australian Labradoodle</p>
        <h2>Better Breed By Design</h2>
        <p class="s-why-body">Adams Farm is an ALAA-registered breeding program, held to a real standard for lineage, health testing, and temperament. Every puppy we place is guaranteed 100% non-shedding and allergy friendly &mdash; the result of that standard, not a coincidence.</p>
      </div>
      <img src="/images/credentials/Better-Breed-By-Design-Badge.png" alt="Better Breed by Design" class="s-why-badge" />
    </div>
    <div class="container s-why-cards">
      <div class="trust-card">
        <h3 class="trust-card-heading">ALAA Registered &mdash; Verified Pedigree</h3>
        <p class="trust-card-body">Every pedigree is verified against ALAA&rsquo;s official database &mdash; the only reliable way to confirm true lineage.</p>
      </div>
      <div class="trust-card">
        <h3 class="trust-card-heading">Multi-Generational</h3>
        <p class="trust-card-body">A true Australian Labradoodle has two Australian Labradoodle parents &mdash; a distinct breed, not a mixed litter.</p>
      </div>
      <div class="trust-card">
        <h3 class="trust-card-heading">Non-Shedding, Allergy-Friendly Coat</h3>
        <p class="trust-card-body">A carefully bred, low-to-no-shedding coat makes the breed allergy friendly.</p>
      </div>
      <div class="trust-card">
        <h3 class="trust-card-heading">Temperament Tested</h3>
        <p class="trust-card-body">Bred for a clever, joyful temperament that reads people &mdash; ideal for therapy and family life.</p>
      </div>
    </div>
    <div class="container s-why-cta">
      <a href="https://alaa-labradoodles.com/for-breeders/breed-standard/" target="_blank" rel="noopener noreferrer" class="trust-program-link">Learn more from ALAA &rarr;</a>
    </div>
  </section>

  <!-- ④ TESTIMONIALS: featured + 2-col grid -->
  <section class="s-testimonials section">
    <div class="container s-section-header">
      <p class="eyebrow">Testimonials</p>
      <h2>Loved by Families</h2>
    </div>
    <div class="container s-testimonials-grid">
      <div class="t-card t-card-featured">
        <blockquote class="t-quote">&ldquo;We are smitten with our bright &amp; adorable Australian Labradoodle from Adam&rsquo;s Farm Labradoodles. Paisley is such a happy girl. She draws attention from people everywhere she goes. She adjusted seamlessly to our home, our routines, and older dog. From the beginning she showed good manners, confidence, and a loving temperament.&rdquo;</blockquote>
        <p class="t-attr">&mdash; Deirdre</p>
      </div>
      <div class="t-card">
        <blockquote class="t-quote">&ldquo;An amazing family operated company. We love our dog, Samson, more than words can describe. He is calm and loving &mdash; we can tell he was born into an environment of care. Doug &amp; Erika are awesome and provided us with all the information we needed as soon as we bought Samson.&rdquo;</blockquote>
        <p class="t-attr">&mdash; Imani-Grace</p>
      </div>
      <div class="t-card">
        <blockquote class="t-quote">&ldquo;We had just moved to the U.S. from Japan, and never expected to meet such a kind and wonderful breeder family so soon. It truly felt like fate. Thanks to Adams Farm, not only did we meet amazing people, but we also welcomed a new, loving member into our family.&rdquo;</blockquote>
        <p class="t-attr">&mdash; Serina</p>
      </div>
    </div>
    <div class="container s-testimonials-cta">
      <a href="https://share.google/FmRRIkSGouEi3RIFT" target="_blank" rel="noopener noreferrer" class="trust-program-link">Read more reviews on Google &rarr;</a>
    </div>
  </section>

  <!-- ⑥ BREAK SECTION: centered full-width quote -->
  <section class="s-break">
    <div class="container s-break-inner">
      <blockquote class="break-quote">
        &ldquo;Dogs are not our whole life,<br class="break-quote-br" /> but they make our lives whole.&rdquo;
        <cite class="break-cite">&mdash; Roger Caras</cite>
      </blockquote>
    </div>
  </section>

  <!-- ⑦ OUR STORY: two-column — text left, image right -->
  <section class="s-story section" id="story">
    <div class="container s-story-inner">
      <div class="s-story-text">
        <p class="eyebrow">Our Story</p>
        <h2>How Adams Farm Began</h2>
        <p class="s-story-body">It started with our kids, Julian and Marie Claire, begging for a puppy. Instead of one, we decided to give them puppies &mdash; and set out to find the right breed. Our only real requirement was temperament: friendly, with no aggression toward people, dogs, or other animals. That search led us to Australian Labradoodles, and eventually to Mike and Pam Kirkpatrick of Legend Manor Labradoodles, and their dog Prancer &mdash; Winnie.</p>
        <p class="s-story-body">From the moment we met her, Erika knew. Winnie approached each of us gently, and it was clear how loving she was &mdash; the kind of dog that becomes a healing presence for a family. She joined ours, and after her third litter, gave us eight healthy puppies. That&rsquo;s how Adams Farm began.</p>
        <blockquote class="s-story-pullquote">
          &ldquo;A great dog can be a healing presence for your whole family.&rdquo;
          <cite class="s-story-cite">&mdash; Erika Campbell</cite>
        </blockquote>
      </div>
      <div class="story-photo">
        <img
          src="/images/story/founder-story-portrait.jpg"
          alt="Douglas and Erika Campbell with Winnie"
          class="story-photo-img"
        />
      </div>
    </div>
  </section>

  <!-- ⑧ CREDENTIALS & PARTNERS: static grid -->
  <section class="s-creds">
    <div class="container s-section-header">
      <p class="eyebrow">Trusted Partners</p>
      <h2>Credentials &amp; Partners</h2>
      <p class="creds-desc">Every Adams Farm puppy is backed by real accreditation and real partnerships &mdash; not just claims. From health testing standards to lifetime microchip registration to the nutrition your puppy grows up on, here&rsquo;s who stands behind our program.</p>
    </div>
    <div class="container creds-grid">
      <div class="creds-card">
        <img src="/images/credentials/sq-gold-paw.png" alt="ALAA Gold Paw Breeder 2026–2027" class="creds-logo" />
        <p class="creds-caption"></p>
      </div>
      <div class="creds-card">
        <img src="/images/credentials/sq-akc-reunite.png" alt="AKC Reunite" class="creds-logo" />
        <p class="creds-caption"></p>
      </div>
      <div class="creds-card">
        <img src="/images/credentials/sq-trupanion.png" alt="Trupanion" class="creds-logo" />
        <p class="creds-caption"></p>
      </div>
      <div class="creds-card">
        <img src="/images/credentials/sq-purina.png" alt="Purina Pro Club" class="creds-logo" />
        <p class="creds-caption"></p>
      </div>
      <div class="creds-card">
        <img src="/images/credentials/sq-puppyq.png" alt="PuppyQ" class="creds-logo" />
        <p class="creds-caption"></p>
      </div>
    </div>
  </section>

  <!-- ⑩ FAQ -->
  <section class="s-faq section">
    <div class="container s-section-header">
      <p class="eyebrow">FAQ</p>
      <h2>Frequently Asked Questions</h2>
    </div>
    <div class="container s-faq-list">

      <details class="faq-item">
        <summary class="faq-question">What is ALAA Gold Paw accreditation, and why does it matter?</summary>
        <div class="faq-answer"><p>Gold Paw is our highest health-testing standard under the Australian Labradoodle Association of America &mdash; the accrediting body that verifies pedigrees and sets the breed standard. It means every breeding dog in our program is health tested and verified, not just labeled &ldquo;Australian Labradoodle.&rdquo;</p></div>
      </details>

      <details class="faq-item">
        <summary class="faq-question">What makes an Australian Labradoodle different from other &ldquo;doodle&rdquo; breeds?</summary>
        <div class="faq-answer"><p>A true Australian Labradoodle is a multi-generational breed with both parents being Australian Labradoodles themselves &mdash; not a first-generation Lab and Poodle mix. Only dogs registered with ALAA, ALCA, or WALA are held to that standard.</p></div>
      </details>

      <details class="faq-item">
        <summary class="faq-question">Are Adams Farm puppies allergy friendly?</summary>
        <div class="faq-answer"><p>Yes. Every puppy we place is guaranteed 100% non-shedding and allergy friendly. As ALAA notes, no dog is truly hypoallergenic, but a carefully bred, low-to-no-shedding coat makes the breed a strong option for allergy-sensitive families.</p></div>
      </details>

      <details class="faq-item">
        <summary class="faq-question">What does &ldquo;raised in loving homes&rdquo; mean &mdash; do all your dogs live with you?</summary>
        <div class="faq-answer"><p>Puppies are whelped in the Campbell home. Some of our breeding dogs live with us, and others live with trusted guardian families &mdash; but every dog in our program is raised in a loving home, never a kennel.</p></div>
      </details>

      <details class="faq-item">
        <summary class="faq-question">What health guarantee comes with my puppy?</summary>
        <div class="faq-answer"><p>We test breeding dogs for known congenital defects and back what can&rsquo;t be tested for with a 3-year written guarantee, so your family can adopt with real confidence.</p></div>
      </details>

    </div>
  </section>

  <!-- ⑪ FOREVER FAMILIES: 7-photo mosaic grid -->
  <section class="s-families">
    <div class="container s-section-header s-section-header-light">
      <p class="eyebrow" style="color: rgba(220,229,219,0.65);">Forever Families</p>
      <h2 style="color: var(--color-card);">Join the Adams Farm Forever Family</h2>
    </div>
    <div class="s-families-grid">
      <img src="/images/families/355x328.png"  alt="Adams Farm forever family with their puppy" class="families-photo" loading="lazy" />
      <img src="/images/families/Slot-2a.png"  alt="Adams Farm forever family with their puppy" class="families-photo" loading="lazy" />
      <img src="/images/families/Slot-3.jpg"   alt="Adams Farm forever family with their puppy" class="families-photo" loading="lazy" />
      <img src="/images/families/Slot-4.png"   alt="Adams Farm forever family with their puppy" class="families-photo" loading="lazy" />
      <img src="/images/families/Slot-2b.png"  alt="Adams Farm forever family with their puppy" class="families-photo" loading="lazy" />
    </div>
  </section>

  <!-- ⑫ APPLY NOW: CTA band with heading and button -->
  <section class="s-apply">
    <div class="container s-apply-inner">
      <p class="eyebrow s-apply-eyebrow">Get Started</p>
      <h2 class="s-apply-heading">Reserve Your Adams Farm Puppy</h2>
      <p class="s-apply-body">Our litters are small and spots fill quickly. Fill out a short application and we'll reach out to discuss your perfect match.</p>
      <a href="/contact" class="btn s-apply-btn">Apply Now</a>
    </div>
  </section>

  <!-- ⑬ FOOTER: Footer.astro (3-column, rendered by Layout) -->

</Layout>

<style>
  /* ─── HERO ──────────────────────────────────────────────────── */
  .hero-section {
    position: relative;
    padding: 72px 0 88px;
    background: var(--color-pine);
  }
  .hero-section .eyebrow { color: rgba(220,229,219,0.7); }
  .hero-section h1 { color: var(--color-card); }
  .hero-section .hero-copy { color: rgba(237,227,208,0.75); }
  .hero-section .btn-primary {
    background: var(--color-card);
    color: var(--color-pine);
    border-color: transparent;
  }
  .hero-section .btn-secondary {
    color: var(--color-card);
    border-color: var(--color-card);
  }
  .hero-grid {
    display: grid;
    grid-template-columns: 0.96fr 1.04fr;
    gap: 56px;
    align-items: center;
  }
  h1 {
    font-size: clamp(2rem, 4vw, 2.8rem);
    line-height: 1.15;
    margin-bottom: 20px;
  }
  .hero-copy {
    color: var(--color-muted);
    font-size: 1.05rem;
    max-width: 440px;
    margin: 0 0 28px;
    line-height: 1.7;
  }
  .hero-actions { display: flex; gap: 12px; flex-wrap: wrap; }
  .hero-photo-frame { position: relative; padding-bottom: 32px; }
  .hero-photo-masked {
    position: relative;
    z-index: 1;
    width: 100%;
    aspect-ratio: 1 / 1;
    clip-path: url(#hero-blob);
    overflow: hidden;
  }
  .hero-photo-img {
    width: 100%; height: 100%;
    object-fit: cover;
    object-position: center 15%;
    display: block;
  }
  .hero-testimonial {
    position: absolute;
    bottom: 0; left: 20px;
    max-width: 320px;
    background: var(--color-white);
    border-radius: 10px;
    padding: 14px 18px;
    box-shadow: 0 6px 28px rgba(0,0,0,0.12);
    z-index: 2;
  }
  .ht-quote {
    font-family: var(--font-display);
    font-style: italic;
    font-size: 0.88rem;
    color: var(--color-pine);
    margin: 0 0 6px;
    line-height: 1.45;
  }
  .ht-attr { font-size: 0.72rem; color: var(--color-muted); margin: 0; }

  /* ─── PLACEHOLDER BASE ──────────────────────────────────────── */
  .ph {
    background: #EDE8DF;
    border: 1.5px dashed #B8A98A;
    border-radius: 6px;
    color: #7A6B52;
    font-size: 0.72rem;
    font-family: var(--font-body);
    font-weight: 700;
    letter-spacing: 0.05em;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 12px 16px;
    line-height: 1.5;
  }

  /* ─── SHARED UTILITIES ──────────────────────────────────────── */
  .section { padding: 72px 0; }
  .s-section-header { margin-bottom: 32px; }
  .ph-label  { min-height: 44px; }
  .ph-tall   { min-height: 360px; }
  .ph-block  { min-height: 180px; }
  .ph-pullquote { min-height: 90px; }

  /* ─── ① PUPPIES ─────────────────────────────────────────────── */
  .s-puppies {
    padding: 64px 0;
    background: var(--color-white);
    border-top: 1px solid var(--color-border-light);
  }
  .s-puppies-hd { margin-bottom: 32px; }
  .puppies-heading { margin: 0 0 var(--space-heading-gap); }
  .puppies-sub {
    color: var(--color-muted);
    font-size: 0.95rem;
    line-height: 1.65;
    max-width: 560px;
    margin: 0 0 var(--space-cta-gap);
  }
  /* shared visual style for all section CTA text links */
  .puppies-parents-link,
  .trust-program-link {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--color-pine);
    text-decoration: none;
    border-bottom: 1.5px solid var(--color-brass);
    padding-bottom: 2px;
  }
  .puppies-cta {
    margin-top: 32px;
  }

  .puppies-carousel-wrap {
    position: relative;
  }
  .carousel-btn {
    position: absolute;
    top: calc(50% - 8px);
    transform: translateY(-50%);
    z-index: 2;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--color-white);
    border: 1.5px solid var(--color-border-light);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 2px 12px rgba(0,0,0,0.10);
    color: var(--color-pine);
    transition: box-shadow 0.15s, background 0.15s;
  }
  .carousel-btn:hover {
    background: var(--color-charcoal-bg);
    box-shadow: 0 4px 18px rgba(0,0,0,0.14);
  }
  .carousel-btn-prev { left: -22px; }
  .carousel-btn-next { right: -22px; }

  .s-scroll {
    display: flex;
    gap: 20px;
    overflow-x: auto;
    padding-bottom: 12px;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
  }
  .s-scroll::-webkit-scrollbar { height: 3px; }
  .s-scroll::-webkit-scrollbar-track { background: var(--color-border-light); border-radius: 2px; }
  .s-scroll::-webkit-scrollbar-thumb { background: var(--color-pine); border-radius: 2px; }

  .puppy-card {
    background: var(--color-charcoal-bg);
    border-radius: 12px;
    overflow: hidden;
    min-width: 220px;
    max-width: 240px;
    flex-shrink: 0;
    scroll-snap-align: start;
  }
  .puppy-card-photo {
    width: 100%;
    aspect-ratio: 3 / 4;
    overflow: hidden;
    background: var(--color-card);
  }
  .puppy-card-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .puppy-card-body {
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 12px 14px;
  }
  .puppy-name {
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--color-cream-d);
  }
  .puppy-collar {
    font-size: 0.7rem;
    color: rgba(247, 242, 231, 0.75);
    margin-top: -4px;
  }
  .puppy-badges {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
  }
  .puppy-sex {
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    padding: 3px 9px;
    border-radius: 20px;
    font-weight: 700;
    background: rgba(255, 255, 255, 0.12);
    color: var(--color-cream-d);
    white-space: nowrap;
  }
  .litter-status {
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    padding: 3px 9px;
    border-radius: 20px;
    font-weight: 700;
    white-space: nowrap;
  }
  .litter-status-available { background: #E1F5EE; color: #085041; }
  .litter-status-reserved  { background: #FAEEDA; color: #633806; }

  @media (max-width: 640px) {
    .carousel-btn { display: none !important; }
  }

  /* ─── PROGRAM SECTION ──────────────────────────────────────── */
  .s-program {
    background: var(--color-charcoal-bg);
    border-top: 1px solid var(--color-border-dark);
  }
  .s-program h2 { color: var(--color-card); }
  .s-program-timeline text { font-family: var(--font-body); }
  .s-program-intro {
    color: rgba(237,227,208,0.75);
    font-size: 1rem;
    line-height: 1.72;
    max-width: 680px;
    margin: 0;
  }
  .s-program-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-top: 40px;
  }
  /* flip card: outer perspective container */
  .prog-card {
    perspective: 1000px;
    height: 260px;
    cursor: pointer;
    outline: none;
  }
  .prog-card:focus-visible {
    border-radius: 12px;
    outline: 2px solid var(--color-pine);
    outline-offset: 3px;
  }
  /* flip card: rotating inner */
  .prog-card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transform-style: preserve-3d;
    transition: transform 0.5s ease;
  }
  .prog-card:hover .prog-card-inner,
  .prog-card:focus .prog-card-inner,
  .prog-card.is-flipped .prog-card-inner {
    transform: rotateY(180deg);
  }
  /* shared face styles */
  .prog-face {
    position: absolute;
    inset: 0;
    backface-visibility: hidden;
    border-radius: 12px;
    padding: 28px;
    overflow: hidden;
  }
  /* front face */
  .prog-face-front {
    background: var(--color-white);
    border: 1px solid var(--color-border-light);
    box-shadow: 0 2px 16px rgba(0,0,0,0.07);
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .prog-card-green  .prog-face-front { border-top: 4px solid var(--color-focus-green); }
  .prog-card-blue   .prog-face-front { border-top: 4px solid var(--color-calm-blue); }
  .prog-card-yellow .prog-face-front { border-top: 4px solid var(--color-excite-yellow); }
  .prog-card-red    .prog-face-front { border-top: 4px solid var(--color-feelings-red); }
  /* back face */
  .prog-face-back {
    transform: rotateY(180deg);
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }
  .prog-card-green  .prog-face-back { background: var(--color-focus-green); }
  .prog-card-blue   .prog-face-back { background: var(--color-calm-blue); }
  .prog-card-yellow .prog-face-back { background: var(--color-excite-yellow); }
  .prog-card-red    .prog-face-back { background: var(--color-feelings-red); }
  .prog-back-body {
    font-size: 0.875rem;
    line-height: 1.65;
    margin: 0;
    color: #fff;
  }
  .prog-card-yellow .prog-back-body { color: var(--color-pine); }
  /* phase label row */
  .prog-phase {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    display: flex;
    align-items: center;
    gap: 8px;
    margin: 0;
  }
  .prog-card-green  .prog-phase { color: var(--color-focus-green); }
  .prog-card-blue   .prog-phase { color: var(--color-calm-blue); }
  .prog-card-yellow .prog-phase { color: #B8921E; }
  .prog-card-red    .prog-phase { color: var(--color-feelings-red); }
  /* phase number badge */
  .prog-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    font-size: 0.68rem;
    font-weight: 700;
    color: #fff;
    flex-shrink: 0;
    text-transform: none;
    letter-spacing: 0;
  }
  .prog-card-green  .prog-badge { background: var(--color-focus-green); }
  .prog-card-blue   .prog-badge { background: var(--color-calm-blue); }
  .prog-card-yellow .prog-badge { background: var(--color-excite-yellow); color: var(--color-pine); }
  .prog-card-red    .prog-badge { background: var(--color-feelings-red); }
  /* card title */
  .prog-title {
    font-family: var(--font-display);
    font-size: 1rem;
    color: var(--color-pine);
    line-height: 1.3;
    margin: 0;
  }
  .prog-timeframe {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--color-muted);
    margin: 0;
  }
  .prog-front-body {
    font-size: 0.78rem;
    color: var(--color-muted);
    line-height: 1.6;
    margin: 0;
  }
  /* reduced motion: crossfade instead of rotate */
  @media (prefers-reduced-motion: reduce) {
    .prog-card-inner { transform: none !important; transition: none; }
    .prog-face { backface-visibility: visible; }
    .prog-face-front { transition: opacity 0.3s; z-index: 1; }
    .prog-face-back  { transform: none; opacity: 0; transition: opacity 0.3s; z-index: 2; }
    .prog-card:hover .prog-face-front,
    .prog-card:focus  .prog-face-front,
    .prog-card.is-flipped .prog-face-front { opacity: 0; }
    .prog-card:hover .prog-face-back,
    .prog-card:focus  .prog-face-back,
    .prog-card.is-flipped .prog-face-back  { opacity: 1; }
  }

  /* ─── ② ABOUT ADAMS FARM ────────────────────────────────────── */
  .s-about { background: var(--color-white); }
  .s-about-inner {
    display: grid;
    grid-template-columns: 2.5fr 3fr;
    gap: 56px;
    align-items: start;
  }
  .s-about-text { display: flex; flex-direction: column; gap: 20px; }
  /* flex gap (20px) is additive with margins; calc() cancels the excess to hit the shared spacing tokens */
  .s-about-text .eyebrow        { margin-bottom: calc(var(--space-eyebrow-gap) - 20px); }
  .s-about-text h2              { margin-bottom: calc(var(--space-heading-gap)  - 20px); }
  .s-about-text .trust-program-link { margin-top: calc(var(--space-cta-gap)    - 20px); }
  .s-about-body {
    color: var(--color-charcoal);
    line-height: 1.78;
    margin: 0;
    font-size: 1rem;
  }
  .about-photo {
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 16px;
    overflow: hidden;
  }
  .about-photo-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
  }

  /* ─── ③ TRUST GRID (inside About section) ───────────────────── */
  .trust-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-top: 8px;
  }
  .trust-card {
    background: var(--color-white);
    border: 1px solid var(--color-border-light);
    border-radius: 12px;
    padding: 28px;
    box-shadow: 0 2px 16px rgba(0,0,0,0.07);
  }
  .trust-icon {
    color: var(--color-brass);
    display: block;
    margin-bottom: 14px;
  }
  .trust-card-heading {
    font-family: var(--font-display);
    font-size: 1rem;
    color: var(--color-pine);
    line-height: 1.3;
    margin: 0 0 10px;
  }
  .trust-card-body {
    font-size: 0.875rem;
    color: var(--color-muted);
    line-height: 1.72;
    margin: 0;
  }
  .trust-program-link {
    display: inline-block;
    align-self: flex-start; /* prevents flex column from stretching the link full-width */
  }

  /* ─── ④ TESTIMONIALS ────────────────────────────────────────── */
  .s-testimonials { background: var(--color-charcoal-bg); }
  .s-testimonials h2 { color: var(--color-card); }
  .s-testimonials .s-testimonials-cta .trust-program-link {
    color: var(--color-card);
    border-bottom-color: var(--color-brass);
  }
  /* wider than --space-cta-gap: this link follows a card grid, not inline text */
  .s-testimonials-cta { margin-top: 28px; }
  .s-testimonials-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }
  .t-card {
    background: var(--color-white);
    border: 1px solid var(--color-border-light);
    border-radius: 12px;
    padding: 32px;
  }
  .t-card-featured { grid-column: 1 / -1; }
  .t-quote {
    margin: 0 0 16px;
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1rem;
    color: var(--color-charcoal);
    line-height: 1.72;
  }
  .t-attr {
    margin: 0;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--color-pine);
  }

  /* ─── ⑤ BREAK / QUOTE ───────────────────────────────────────── */
  .s-break {
    background: var(--color-pine);
    padding: 80px 0;
  }
  .s-break-inner { text-align: center; max-width: 680px; margin: 0 auto; }
  .break-quote {
    margin: 0;
    font-family: var(--font-display);
    font-style: italic;
    font-size: clamp(1.5rem, 3vw, 2.1rem);
    color: var(--color-card);
    line-height: 1.55;
  }
  .break-cite {
    display: block;
    font-style: normal;
    font-family: var(--font-body);
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    color: rgba(220,229,219,0.55);
    margin-top: 20px;
  }
  @media (max-width: 600px) {
    .break-quote-br { display: none; }
  }

  /* ─── ⑤ WHY AUSTRALIAN LABRADOODLE ─────────────────────────── */
  .s-why { background: var(--color-white); }
  .s-why-inner {
    display: grid;
    grid-template-columns: 3fr 2.5fr;
    gap: 56px;
    align-items: center;
  }
  .s-why-text { display: flex; flex-direction: column; gap: 20px; }
  .s-why-text .eyebrow { margin-bottom: calc(var(--space-eyebrow-gap) - 20px); }
  .s-why-text h2       { margin-bottom: calc(var(--space-heading-gap)  - 20px); }
  .s-why-body {
    color: var(--color-charcoal);
    line-height: 1.78;
    margin: 0;
    font-size: 1rem;
  }
  .s-why-badge {
    width: 100%;
    max-width: 240px;
    height: auto;
    display: block;
    margin: 0 auto;
  }
  .s-why-cards {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 48px;
  }
  .s-why-cta {
    margin-top: 28px;
  }

  /* ─── ⑦ OUR STORY ───────────────────────────────────────────── */
  .s-story { background: var(--color-charcoal-bg); }
  .s-story h2 { color: var(--color-card); }
  .s-story-body { color: rgba(237,227,208,0.8); }
  .s-story-pullquote { color: var(--color-card); border-left-color: var(--color-brass); }
  .s-story-cite { color: rgba(237,227,208,0.55); }
  .s-story-inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    gap: 64px;
    align-items: start;
  }
  .story-photo {
    width: 100%;
    aspect-ratio: 3 / 4;
    border-radius: 16px;
    overflow: hidden;
  }
  .story-photo-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    display: block;
  }
  .s-story-text { display: flex; flex-direction: column; gap: 24px; }
  .s-story-text .eyebrow { margin-bottom: calc(var(--space-eyebrow-gap) - 24px); }
  .s-story-text h2       { margin-bottom: calc(var(--space-heading-gap)  - 24px); }
  .s-story-body {
    color: var(--color-charcoal);
    line-height: 1.78;
    margin: 0;
    font-size: 1rem;
  }
  .s-story-pullquote {
    margin: 0;
    padding: 20px 24px;
    border-left: 3px solid var(--color-brass);
    font-family: var(--font-display);
    font-style: italic;
    font-size: 1.1rem;
    color: var(--color-pine);
    line-height: 1.5;
  }
  .s-story-cite {
    display: block;
    font-style: normal;
    font-family: var(--font-body);
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    color: var(--color-muted);
    margin-top: 10px;
  }

  .s-section-header { padding: 0 24px; }

  /* ─── ⑧ CREDENTIALS & PARTNERS ─────────────────────────────── */
  .creds-desc {
    color: rgba(237,227,208,0.75);
    font-size: 0.95rem;
    line-height: 1.72;
    max-width: 680px;
    margin: 0;
  }
  .s-creds {
    padding: 72px 0;
    background: var(--color-charcoal-bg);
    border-top: 1px solid var(--color-border-dark);
    border-bottom: 1px solid var(--color-border-dark);
  }
  .s-creds h2 { color: var(--color-card); }
  .creds-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 16px;
    margin-top: 40px;
  }
  .creds-card {
    background: var(--color-pine);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 12px;
    padding: 28px 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    text-align: center;
    box-shadow: 0 2px 16px rgba(0,0,0,0.18);
  }
  .creds-logo {
    width: 100%;
    height: auto;
    display: block;
  }
  .creds-caption {
    font-size: 0.825rem;
    color: var(--color-muted);
    line-height: 1.6;
    margin: 0;
  }

  /* ─── ⑩ FAQ ─────────────────────────────────────────────────── */
  .s-faq { background: var(--color-white); }
  .s-faq-list { display: flex; flex-direction: column; gap: 8px; }
  .faq-item {
    background: var(--color-white);
    border: 1px solid var(--color-border-light);
    border-radius: 12px;
    overflow: hidden;
  }
  .faq-question {
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    padding: 20px 24px;
    font-family: var(--font-display);
    font-size: 1rem;
    color: var(--color-pine);
    line-height: 1.4;
  }
  .faq-question::-webkit-details-marker { display: none; }
  .faq-question::after {
    content: '+';
    font-size: 1.4rem;
    font-weight: 300;
    color: var(--color-brass);
    flex-shrink: 0;
    transition: transform 0.2s ease;
  }
  details[open] > .faq-question::after { transform: rotate(45deg); }
  .faq-answer {
    padding: 0 24px 20px;
    border-top: 1px solid var(--color-border-light);
  }
  .faq-answer p {
    margin: 16px 0 0;
    color: var(--color-charcoal);
    font-size: 0.95rem;
    line-height: 1.78;
  }

  /* ─── ⑪ FOREVER FAMILIES ────────────────────────────────────── */
  .s-families {
    background: var(--color-pine);
    padding-top: 64px;
    padding-bottom: 64px;
  }
  .s-section-header-light { margin-bottom: 32px; }
  .ph-label-dark {
    min-height: 44px;
    background: rgba(220,229,219,0.07);
    border-color: rgba(220,229,219,0.2);
    color: rgba(220,229,219,0.55);
  }
  .s-families-grid {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    grid-template-rows: 328px 160px;
    gap: 8px;
    max-width: 1080px;
    margin: 0 auto;
    padding: 0 24px;
  }
  .families-photo {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  /* 355x328 — tall left anchor, spans 2 cols × 2 rows */
  .families-photo:nth-child(1) { grid-column: 1 / 3; grid-row: 1 / 3; }
  /* Slot-2a — wide top right, spans 4 cols */
  .families-photo:nth-child(2) { grid-column: 3 / 7; grid-row: 1; }
  /* Slot-3, Slot-4 — square cells; Slot-2b — landscape, spans 2 cols */
  .families-photo:nth-child(3) { grid-column: 3; grid-row: 2; object-position: center 80%; }
  .families-photo:nth-child(4) { grid-column: 4; grid-row: 2; }
  .families-photo:nth-child(5) { grid-column: 5 / 7; grid-row: 2; }

  /* ─── ⑫ APPLY NOW ───────────────────────────────────────────── */
  .s-apply {
    background: var(--color-brass);
    padding: 80px 0;
  }
  .s-apply-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }
  .s-apply-eyebrow {
    color: rgba(220,229,219,0.65);
    margin-bottom: var(--space-eyebrow-gap);
  }
  .s-apply-heading {
    color: var(--color-card);
    margin-bottom: 16px;
  }
  .s-apply-body {
    color: rgba(220,229,219,0.85);
    max-width: 520px;
    margin: 0 0 36px;
    font-size: 1rem;
    line-height: 1.6;
  }
  .s-apply-btn {
    background: var(--color-card);
    color: var(--color-pine);
    border-color: transparent;
    padding: 14px 36px;
    font-size: 0.95rem;
  }
  .s-apply-btn:hover {
    background: var(--color-white);
  }

  /* ─── RESPONSIVE ────────────────────────────────────────────── */

  /* puppy carousel: 3 cards visible on desktop */
  @media (min-width: 1024px) {
    .puppies-carousel-wrap .puppy-card {
      width: calc((100% - 40px) / 3);
      min-width: 0;
      max-width: none;
    }
  }
  /* puppy carousel: 2 cards visible on tablet */
  @media (min-width: 768px) and (max-width: 1023px) {
    .puppies-carousel-wrap .puppy-card {
      width: calc((100% - 20px) / 2);
      min-width: 0;
      max-width: none;
    }
  }
  /* puppy carousel: vertical stack on mobile */
  @media (max-width: 767px) {
    .puppies-carousel-wrap .s-scroll {
      flex-wrap: wrap;
      overflow-x: visible;
      scroll-snap-type: none;
    }
    .puppies-carousel-wrap .puppy-card {
      width: 100%;
      min-width: 0;
      max-width: none;
      flex-shrink: 1;
      scroll-snap-align: none;
    }
    .puppies-carousel-wrap .carousel-btn { display: none !important; }
    .puppy-card:not([data-status="available"]) { display: none; }
  }

  @media (max-width: 900px) {
    .s-program-grid   { grid-template-columns: 1fr 1fr; }
    .creds-grid       { grid-template-columns: repeat(3, 1fr); }
    .s-about-inner    { grid-template-columns: 1fr; }
    .s-why-inner      { grid-template-columns: 1fr; }
    .s-why-badge      { max-width: 180px; margin: 0; }
    .s-why-cards      { grid-template-columns: 1fr 1fr; }
    .s-story-inner    { grid-template-columns: 1fr; }
    .s-testimonials-grid { grid-template-columns: 1fr; }

    .s-families-grid {
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: 220px 140px 140px;
    }
    .families-photo:nth-child(1) { grid-column: 1 / 3; grid-row: 1 / 3; }
    .families-photo:nth-child(2) { grid-column: 3; grid-row: 1; }
    .families-photo:nth-child(3) { grid-column: 3; grid-row: 2; }
    .families-photo:nth-child(4) { grid-column: 1; grid-row: 3; }
    .families-photo:nth-child(5) { grid-column: 2 / 4; grid-row: 3; }
  }
  @media (max-width: 640px) {
    .hero-section { padding: 48px 0 60px; }
    .hero-grid { grid-template-columns: 1fr; gap: 32px; }
    .hero-photo-frame { max-width: 360px; margin: 0 auto; }
    .hero-testimonial { left: 8px; max-width: calc(100% - 16px); }
    .s-program-grid   { grid-template-columns: 1fr; }
    .creds-grid       { grid-template-columns: repeat(2, 1fr); }
    .trust-grid { grid-template-columns: 1fr; }
    .s-why-cards { grid-template-columns: 1fr; }
    .s-testimonials-grid { grid-template-columns: 1fr; }
    .s-families-grid {
      grid-template-columns: 1fr;
      grid-template-rows: repeat(5, 200px);
    }
    .families-photo { grid-column: 1 !important; grid-row: auto !important; }
    .ph-logo { width: 100px; height: 60px; }
  }
</style>
